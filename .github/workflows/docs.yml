name: Deploy docs

# Rebuild the site (which regenerates the knowledge graph + live stats from the corpus)
# and redeploy it to the public Pages repo on every push to main — so the graph stays
# current as the corpus grows (incl. the nightly auto-ingest commits).
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-deploy
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: pip install -r website/requirements.txt
      - name: Build (regenerates graph-data.json + live stats)
        run: cd website && mkdocs build --strict
      - name: Publish to public Pages repo
        env:
          TOKEN: ${{ secrets.DOCS_DEPLOY_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "::notice::DOCS_DEPLOY_TOKEN not set — built & validated, skipping deploy."
            exit 0
          fi
          cd website/site
          touch .nojekyll
          git init -q && git checkout -q -b gh-pages && git add -A
          git -c user.email=ci@corpus -c user.name=corpus-ci commit -q -m "deploy: ${GITHUB_SHA::7}"
          git push -fq "https://x-access-token:${TOKEN}@github.com/joaoblasques/corpus-docs.git" gh-pages
          echo "::notice::Deployed to https://joaoblasques.github.io/corpus-docs/"
